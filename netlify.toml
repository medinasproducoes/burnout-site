[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Protege o PDF contra acesso direto — só a página de obrigado libera
[[headers]]
  for = "/assets/burnout-em-silencio.pdf"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
